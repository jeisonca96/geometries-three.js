// sphere object
const geometrySphere = new THREE.SphereGeometry(0.7, 10, 10);
const materialSphere = new THREE.MeshBasicMaterial({ color: "#FE975D" });
const sphere = new THREE.Mesh(geometrySphere, materialSphere);
